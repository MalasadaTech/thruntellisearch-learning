title: ThruntelliSearch Learning     # This shows as the main header / site title (cleaner than raw repo name)
description: A collection of ThruntelliSearch notes and learning materials

theme: minima
minima:
  skin: dark                        # Or 'auto' for system preference

header_pages:                       # This controls the top-right nav links (in order)
  - index.md                        # → Becomes "Home" or uses title from front matter
  - links.md                        # → Becomes "Links" or uses title from front matter